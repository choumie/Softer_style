import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'softer_style',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
